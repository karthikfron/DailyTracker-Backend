Completed (Till Day 4)
1. Core Authentication System

Implemented secure signup and login with bcrypt password hashing.

Added JWT-based access tokens (1-hour validity).

Built refresh-token rotation with a 7-day validity.

Added /token endpoint for seamless token renewal.

Implemented logout with refresh-token invalidation.

Added JWT middleware to protect user-specific routes like /profile.